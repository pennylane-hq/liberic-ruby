# Generated by projectsetup. Edit projectsetup.json and regenerate instead of editing this file.
name: liberic-ruby

services:
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    command: sleep infinity
    user: vscode
    volumes:
      - ..:/workspaces/liberic-ruby
